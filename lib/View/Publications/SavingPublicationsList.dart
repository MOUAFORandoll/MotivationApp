import 'package:flutter/material.dart';

class SavingPublicationListScreen extends StatelessWidget {
  const SavingPublicationListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
